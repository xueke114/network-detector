{
  "name": "network-detector",
  "displayName": "Network Detector",
  "description": "检测网络连接状态",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Network Detector",
      "properties": {
        "networkDetector.targetAddress": {
          "type": "string",
          "default": "www.baidu.com",
          "description": "检测地址"
        },
        "networkDetector.checkInterval": {
          "type": "number",
          "default": 5000,
          "description": "检测间隔时间（毫秒）"
        },
        "networkDetector.statusIcon": {
          "type": "string",
          "default": "⬤",
          "description": "状态显示图标"
        }
      }
    },
    "languages": [
      {
        "id": "network-detector",
        "extensions": [
          ".json"
        ]
      }
    ],
    "translations": [
      {
        "languageId": "zh-cn",
        "path": "package.nls.zh-cn.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@vscode/test-electron": "^2.1.5",
    "eslint": "^8.26.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.8.4",
    "vsce": "^2.15.0"
  },
  "volta": {
    "node": "24.13.0",
    "pnpm": "10.28.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/xueke114/network-detector.git"
  }
}